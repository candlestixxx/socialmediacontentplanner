# IDEAS

- **SQLite Transition:** Formally migrate the local and test environments to SQLite natively using Prisma's multi-schema/multi-provider capabilities instead of custom proxying.
- **Mobile Native Preview Renderer:** Build a visual post customizer on mobile that displays real pixel previews matching target social networks (e.g. mock Twitter cards or Instagram layout frames).
